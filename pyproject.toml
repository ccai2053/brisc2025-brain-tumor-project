[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "brisc-project"
version = "0.1.0"
dependencies = [
    "torch",
    "torchvision",
    "matplotlib",
    "jupyter",
    "pillow"
]

[tool.setuptools.packages.find]
where = ["src"]